import { ExpenseCategory } from '../types/expenses';

// No default seed categories. All categories come from Planner SSOT or user creation.
export const DEFAULT_EXPENSE_CATEGORIES: ExpenseCategory[] = [];
